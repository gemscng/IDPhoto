import { PaymentCancelledContent } from "@/components/payment-cancelled-content";

export const dynamic = "force-dynamic";

export default function PaymentCancelledPage() {
  return <PaymentCancelledContent />;
}
